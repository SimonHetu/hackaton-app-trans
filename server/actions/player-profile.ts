"use server";
import { PlayerProfileInput } from "@/lib/validations/player-profile";

export async function updatePlayerProfile(input: PlayerProfileInput) {
  // TODO: implémenté par le back
  console.log("TODO updatePlayerProfile", input);
  return { success: true };
}